// Link vector icon fonts as assets.
// After installing dependencies, run:
//   npx react-native-asset
// iOS users should also run:
//   cd ios && pod install

module.exports = {
  assets: ['./node_modules/react-native-vector-icons/Fonts'],
};
