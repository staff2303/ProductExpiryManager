// src/theme/metrics.ts
/**
 * ✅ 버튼/입력/아이콘버튼 "줄인" 고정 규격
 * - 여기 값은 스케일 함수(sp/sz/fs/is) 쓰지 말고 고정값으로 유지
 */
export const M = {
  // pill 버튼(DB, +추가)
  pillH: 44,
  pillPadX: 12,
  pillPadY: 8,

  // 입력/검색 라인(검색박스/스캔 버튼 높이)
  inputH: 48,
  inputPadX: 10,
  inputPadY: 8,

  // 아이콘 버튼(수정/삭제/뒤로)
  iconBtn: 44,
  iconRadius: 12,

  // 검색어 지우기(✕)
  clearBtn: 40,
  clearRadius: 12,

  // 날짜필터 닫기(✕)
  closeBtn: 48,
  closeRadius: 12,
};
